import React, {Component} from 'react';
import { ButtonToolbar, Button} from 'react-bootstrap';

class SeeNotesButton extends Component {
    render() {
        return (
          <ButtonToolbar>
              <Button>See Notes</Button>
          </ButtonToolbar>
        )
    }
}

export default SeeNotesButton
